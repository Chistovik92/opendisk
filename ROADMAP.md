# Дорожная карта OpenDisk

Открытый кроссплатформенный клиент виртуальных облачных дисков (аналог Disk-O / Air Explorer),
без подписок, на движке [rclone](https://rclone.org/) с собственным GUI.

Реализацию ведёт Claude по запросам [[SecretHero]](https://github.com/Chistovik92) —
каждый этап оформляется отдельными PR/коммитами с рабочим кодом, а не только описанием.

## Принцип архитектуры

Диски выглядят как обычные физические (буква диска в Windows, том в Finder, папка в
файловом менеджере Linux, источник в «Файлах» на Android/iOS), но полностью виртуальны:
содержимое не хранится на устройстве, а подгружается по требованию из облака.

Десктоп и мобильные платформы устроены принципиально по-разному — у Android/iOS нет
доступа к FUSE/WinFsp без root/jailbreak, вместо этого у них есть свои официальные
API именно для облачных дисков. Поэтому архитектура состоит из общего ядра и разных
"обвязок" под каждую платформу:

```
                         ┌──────────────────────────┐
                         │      rclone (Go)           │  общее ядро для ВСЕХ платформ:
                         │  протоколы облаков, OAuth,  │  протоколы, кэш, шифрование —
                         │  кэш, шифрование            │  ничего не пишем заново
                         └─────────────┬────────────┘
                                       │
            ┌──────────────────────────┼──────────────────────────┐
            │ Десктоп (Win/Linux/macOS) │      Мобильные (Android/iOS)          │
            │ rclone rcd как процесс,   │      rclone-core собран как           │
            │ GUI общается по HTTP RC   │      библиотека через gomobile        │
            │ API                        │      (.aar / .framework)              │
            └──────────────┬────────────┘      └──────────────┬─────────────────┘
                           │                                    │
        ┌──────────────────▼──────────────────┐   ┌────────────▼─────────────────┐
        │  Виртуальная ФС уровня ОС             │   │  Официальный API облачных     │
        │  Linux: FUSE                           │   │  дисков платформы              │
        │  macOS: macFUSE                        │   │  Android: DocumentsProvider    │
        │  Windows: WinFsp                       │   │  iOS: File Provider Extension  │
        │  → виден ЛЮБОМУ приложению в ОС        │   │  → виден в «Файлах», доступен   │
        │                                         │   │    приложениям через системный │
        │                                         │   │    диалог выбора файла         │
        └─────────────────────────────────────────┘   └─────────────────────────────────┘
```

**Важное отличие мобильных платформ:** на десктопе диск монтируется как настоящая
папка, видимая абсолютно всем программам. На Android/iOS так низкоуровнево работать
запрещено самой ОС — вместо этого используется официальный, предназначенный именно
для облачных дисков механизм (так же устроены официальные клиенты Google Drive и
Dropbox на телефонах). Внешне для пользователя разница минимальна — диск так же
виден в «Файлах» с on-demand загрузкой, — но интеграция с произвольными приложениями
идёт не напрямую через ФС, а через системный диалог выбора файлов.

Мы нигде не переизобретаем протоколы облаков и монтирование — за это отвечает rclone
(70+ провайдеров, включая Яндекс.Диск, Mail.ru через WebDAV, Google Drive, S3, Dropbox)
на всех платформах одновременно. Наша задача — GUI/нативная обвязка под каждую ОС,
автозапуск, трей, профили, кэш-политики.

### Что общего у платформ на самом деле

Общий не только rclone, но и наш код поверх него. Разбор ответов RC API — один
класс `RcloneClient` в модуле `rclone-bridge`, а различается только доставка вызова:
на десктопе HTTP до соседнего процесса `rclone rcd`, на Android — функция внутри
процесса через librclone. Всё, что добавляется в этот класс, появляется на всех
платформах сразу; так, ссылки на файлы (0.4.0) писались один раз и на Android
уже работают, хотя интерфейса там ещё нет.

---

## Как читать эту карту

Раньше она была разбита на «этапы» — от них пришлось отказаться: этапы шли не по
порядку, а версии выходили чаще, чем закрывались этапы. Теперь всё по версиям
приложения, и это заодно отвечает на вопрос «что уже можно поставить».

Сделанное описано коротко: подробности каждого выпуска —
на [странице релизов](https://github.com/Chistovik92/opendisk/releases).
Незакрытые пункты сгруппированы по будущим версиям; сроки не проставлены
намеренно, порядок важнее дат.

---

## Выпущено

### 0.1.x — рабочий десктоп

Ядро и интерфейс. К концу ветки приложение делало всё, ради чего затевалось.

- **Ядро** `rclone-bridge`: запуск и остановка `rclone rcd`, типизированная
  обёртка над RC API, разбор ошибок rclone в человеческий текст.
- **Встроенный rclone**: едет внутри дистрибутива, скачивается при сборке
  и сверяется по SHA-256. Отдельно ставить ничего не нужно.
- **Интерфейс** на Compose Multiplatform: список облаков, мастер добавления,
  подключение диском, занятое место, трей, уведомления.
- **Плитки популярных сервисов** вместо списка из семи десятков бэкендов;
  полный список остался за «другим подключением».
- **Монтирование**: WinFsp едет в дистрибутиве Windows и ставится по кнопке;
  на Linux `fuse3` прописан в зависимостях пакета.
- **Режимы кэша** — ответ на главный вопрос «скачивать всё или по требованию».
- **Автозапуск, автоподключение, ограничение скорости, русский и английский.**
- **Упаковка**: MSI (свой установщик на WiX — jpackage не умеет закрывать
  работающее приложение), DEB, RPM, AppImage, DMG; сборка в CI на трёх ОС.
- **Установка на Linux одной командой** скриптом с GitHub.
- **Окно «О приложении»**, руководство пользователя, страница проекта.

### 0.2.x — то, что вскрылось при реальной эксплуатации

Ветка целиком состоит из починок, и почти каждая нашлась не по коду, а по жалобе
или замеру на живой системе.

- **0.2.5** — обновление из приложения со сверкой контрольной суммы; уборка
  за собой при удалении, с отдельным спросом про конфиг облаков и WinFsp.
- **0.2.6** — диски на Windows стали сетевыми: как обычные локальные они
  заставляли систему их индексировать, и медленное облако подвешивало
  проводник целиком. Предупреждение про общий идентификатор Google:
  замер на живом диске дал 33 секунды на список из 65 файлов против одной
  секунды у Яндекса.
- **0.2.7** — неверный идентификатор Google ловится до похода в браузер;
  из ожидания подтверждения можно выйти.
- **0.2.8** — поле подключения можно очистить. Без этого нельзя было уйти
  с удалённых учётных данных Google.
- **0.2.9** — rpm собирался неправильно: встроенный rclone был неисполняемым,
  ярлык пропадал после каждого обновления, работающая копия не закрывалась.

Кроме того, в эту же ветку попали починки, каждая из которых была настоящей
поломкой: автозапуск не работал ни в одной версии; на Linux не создавался
каталог точки монтирования; осиротевший rclone после аварийного завершения
держал диски.

### 0.3.x — поставилось и нашлось

- **0.3.0** — галка «Запустить OpenDisk» на последней странице установщика
  Windows, отмеченная по умолчанию; обновление изнутри приложения запускает
  новую версию само; на Linux пакет возвращает приложение, если сам же его
  закрыл ради обновления.
- **0.3.1** — в строке состояния сказано, что сетевой диск лежит в разделе
  «Сетевые расположения». До этого человек открывал «Этот компьютер», среди
  обычных дисков ничего не находил и решал, что диск не смонтировался.

### 0.4.x — ссылки на файлы

Первая возможность, а не починка, за две ветки. Вопрос был простой: файл лежит
на подключённом диске, надо дать на него ссылку — и до сих пор для этого
приходилось идти на сайт сервиса.

- **Ссылку выдаёт то облако, которое файл действительно держит.** Приложение
  раскладывает местный путь (`Z:\Отчёты\март.pdf`) обратно на облако и путь
  внутри него (`яндекс` + `Отчёты/март.pdf`) и просит ссылку у нужного сервиса.
  Диск определяется по самому пути: в файловом диалоге можно уйти на соседний
  подключённый диск, и ссылка придёт от него, а не от того, с чьей кнопки начали.
- **Кнопка есть только там, где ссылки возможны.** Умеет ли бэкенд ссылки,
  спрашивается у самого rclone (`operations/fsinfo`), а не берётся из зашитой
  в код таблицы, которая устаревала бы молча. У Яндекс.Диска, Google Диска,
  Dropbox и OneDrive ссылки есть; у SFTP, FTP и обычного WebDAV их нет в самом
  протоколе — и об этом говорится словами, а не кодом ошибки.
- **Ссылку можно отозвать оттуда же, откуда выдали.** Сделать файл публичным
  легко, и закрыть доступ должно быть так же легко; иначе остаётся только идти
  на сайт сервиса. Файл при этом остаётся на месте.
- **Написано в общем модуле — значит, работает и на Android.** `publicLink`,
  `fsInfo` и разбор пути лежат в `rclone-bridge`, который на телефоне тот же
  самый; инструментальные тесты гоняют их через librclone на эмуляторе.
  Интерфейса на Android пока нет, но ядро возможности уже есть и проверено.

**Неполный релиз больше не может уехать наружу.** Раньше каждая сборка сама
делала релиз видимым, как только доходила до конца, — и релиз становился
публичным от первой успевшей платформы. Теперь релиз создаётся черновиком, а
публикует его отдельная работа после всех трёх сборок сразу и только убедившись,
что в нём есть установщики под каждую платформу. Упала хоть одна — релиз
остаётся черновиком, и пользователи его не видят.

**0.4.1** — тем же выпуском, потому что 0.4.0 доехал только до Windows.
Новые тесты сверяли сообщение об ошибке с русской подстрокой, а язык
интерфейса берётся из системы: на англоязычных раннерах Linux и macOS то же
самое сообщение приходило по-английски, тесты падали и сборка пакетов до
выкладывания не доходила. Сверка теперь идёт с той строкой, которую берёт сам
контроллер, — двуязычие перестало быть ловушкой для тестов.

### 0.5.0 — установщик под каждую машину

Выпуск целиком про упаковку: приложение то же, а вот получить его должно быть
одинаково просто на любой машине.

- **На Windows теперь `.exe`, а не `.msi`.** MSI никуда не делся — он уехал
  внутрь exe: вся выстраданная логика установщика (закрытие работающего
  приложения при обновлении, стабильные GUID-ы компонентов, уборка при
  удалении) осталась в нём, переписывать её ради смены расширения было бы
  напрасным риском. Снаружи — привычный установщик, который и Windows, и
  браузеры встречают спокойнее, чем MSI.
- **Windows под ARM.** Отдельный `.exe` для ноутбуков на ARM: своя JDK, свой
  rclone, своя сборка. Раньше такие машины могли только эмулировать x64.
  Сама сборка 0.5.0 на ARM не запускалась — это нашла проверка в 0.5.2.
  В 0.5.2 ARM не выпущена, см. 0.5.3.
- **macOS в двух архитектурах.** Отдельные `.dmg` для Apple Silicon и для
  Intel — до этого собирался один, и какой именно, зависело от того, какой
  раннер сегодня у GitHub.
- **Свой пакет для ALT Linux и Simply Linux.** ALT — не производная Fedora
  и не совместим с ней по именам зависимостей: rpm от jpackage там либо не
  ставился, либо тянул не то. Теперь его собирает контейнер самого ALT.
- **Скрипт установки смотрит на дистрибутив, а не только на пакетный
  менеджер.** Debian и Ubuntu получают `.deb`, ALT и Simply — свой rpm,
  Fedora и openSUSE — свой, всё остальное — AppImage, который не зависит
  ни от какого пакетного менеджера. Выбор проверяется тестом на файлах
  `/etc/os-release` полутора десятков дистрибутивов: живых систем для проверки
  нет и не будет, а их os-release известен.
- **Android: приложение, а не только ядро.** Список облаков с занятым местом,
  добавление облака логином и паролем, просмотр по папкам, ссылка на файл —
  всё на общем с десктопом коде. В релизе два `.apk`: под arm64 и
  универсальный. Диска в системном «Файлы» пока нет — это `DocumentsProvider`,
  и он в 0.6.

**Что «все платформы» значит и чего не значит.** Установщики теперь есть под
Windows (x64 и ARM), Linux (deb, два rpm, AppImage), macOS (Intel и Apple
Silicon) и Android. Под iOS установщика нет и в этом выпуске быть не может:
файл, который ставится на iPhone, обязан быть подписан сертификатом Apple,
а сертификат выдаётся только участнику платной программы разработчика.
Это не техническая задача и обходится не кодом. Собрано и проверено то, что
собрать можно: ядро rclone под iOS (`.xcframework`, устройства и симулятор).

**0.5.2 — то, что 0.5.0 сломал молча, и проверка, которая этого больше не
допустит.** Переход на `.exe` сломал две вещи, о которых узнать было
неоткуда: встроенное обновление искало `.msi` и перестало ставить что-либо,
а удаление изнутри приложения сносило скрытый MSI и оставляло в «Программах
и компонентах» запись-сироту. Сценарии установки и удаления вынесены в файлы,
и CI гоняет ровно их на настоящей Windows: старый `.msi` →
прошлый `.exe` → обновление запущенного приложения → запуск → удаление.
Linux-пакеты перед выпуском ставятся тем самым `install.sh` в чистых Debian,
Ubuntu 22.04 и 24.04, Fedora, openSUSE и ALT, с обновлением с прошлой версии.
Там же нашлись и исправлены пакет ALT, который требовал не тот FUSE и вёз
неисполняемый rclone, и Android — пароль без маски, облако нельзя было
удалить, код версии навсегда оставался единицей.

---

## Дальше

### 0.5.3 — телефоны и то, что мешало

Выпуск про мобильные платформы и про ошибки, а не про новые возможности
десктопа. Что сделано:

- [x] **Подключение на Android.** Облако отдаётся системе поставщиком
      документов и видно в «Файлах» и в любом окне выбора файла. Диском, как
      на компьютере, оно быть не может: файловые системы монтирует ядро,
      приложению этого не позволено — это ограничение платформы, одинаковое
      и для официальных клиентов Google Drive и Dropbox. Только чтение:
      заливка из чужого приложения в фоне — отдельная работа.
- [x] **Значок в шторке**, пока облака подключены: на телефоне это
      единственное место, где видно, что приложение работает, — система ходит
      за файлами и без открытого экрана.
- [x] **Выбор сервиса на телефоне**, как на компьютере: Яндекс.Диск, Mail.ru,
      WebDAV, SFTP, FTP с готовыми адресами и предупреждением про пароль
      приложения. Раньше здесь был список из трёх слов, и знать, что Яндекс
      подключается по WebDAV и по какому адресу, должен был человек.
- [x] **Приложение для iPhone — без подписи.** Сертификата Apple у проекта
      нет и не будет без платной программы разработчика, но неподписанный
      `.ipa` всё же ставится: его подписывает своей учётной записью тот, кто
      ставит. Ценой семи дней жизни подписи и трёх приложений одновременно;
      см. [docs/IOS.md](docs/IOS.md), там же про разницу между ЕС
      (альтернативные магазины по DMA) и остальным миром.
- [x] **Тема из системы на всех платформах.** На Android 12 и новее оттуда
      же берётся вся палитра, построенная от обоев; на десктопе тема и цвет
      выделения читаются реестром, `defaults` и `gsettings` — Compose там
      системной темы не знает вовсе.
- [x] **Настройки переделаны.** На десктопе диалог обрезался по нижнему
      разделу — на экране оставалась включённая галочка без подписи, и понять,
      что она делает, было нельзя. Теперь он прокручивается и разбит на
      разделы, «Проверить обновления» и «Удалить OpenDisk» переехали туда из
      «О приложении», а в меню трея появился пункт «Настройки». На телефонах
      настройки стали отдельным экраном.

Что осталось:

- [x] Сервисы с подтверждением доступа в браузере на телефонах — сделано
      в 0.5.4, см. ниже.
- [ ] Запись в облако с телефона: сейчас подключённое облако доступно только
      на чтение.

### 0.5.4 — вход через браузер на телефонах и полный список сервисов

- [x] **Вход через браузер на Android и iPhone.** Пароль вводить и тем более
      создавать не нужно: выбрать аккаунт, разрешить доступ — и облако в
      списке. Работает у всех сервисов, которые подтверждают доступ в
      браузере, без единой строчки под каждый: как входить, знает rclone.
      Он работает внутри приложения, поднимает там же сервер подтверждения
      и печатает ссылку в свой журнал; приложение перехватывает журнал
      (дескриптор потока ошибок — проверено по исходникам rclone и gomobile)
      и открывает ссылку во вкладке Custom Tabs на Android и в окне входа
      Apple на iPhone. Встроенное окно не годится: Google не пускает на вход
      из встроенных браузеров.
- [x] **Отмена входа без хвостов** — на телефонах и на компьютере. Висящий
      запрос будится отказом по той же ссылке, недосозданное облако
      удаляется. На компьютере раньше и rclone продолжал ждать браузера,
      и облако без токена оставалось в списке.
- [x] **Один каталог сервисов на все платформы** (`CloudCatalog` в
      `rclone-bridge`, для iOS — выгрузкой в JSON). Около сорока отобранных
      сервисов с готовыми адресами — от Google Диска и Proton Drive до
      Selectel и Hetzner — и ниже всё, что знает встроенный rclone, с каждым
      провайдером S3 отдельной строкой. Поиск — по названию, стране и другому
      имени. Проверка сверяет каталог с настоящим rclone: опечатку в поле
      rclone проглотил бы молча.

Что осталось:

- [ ] Вход через браузер у сервисов, где rclone спрашивает что-то посреди
      входа: код двухфакторной защиты iCloud, личный токен Jottacloud. Такие
      сервисы есть в полном списке, но добавление закончится ошибкой rclone.
- [x] Возврат в приложение после входа на Android — в 0.5.5, нажатием на
      значок в шторке.

### 0.5.5 — вход через браузер на настоящем телефоне и значок в шторке

- [x] **Вход через браузер обрывался на последнем шаге.** На настоящем
      телефоне вход в Яндекс.Диск дошёл до обмена кода на токен и упал на
      «lookup oauth.yandex.com: no such host». DNS в librclone исправен —
      проверка на эмуляторе на переднем плане резолвит имя и читает по HTTPS.
      Дело в фоне: обмен идёт, когда впереди вкладка браузера, а фоновому
      приложению многие телефоны отрезают сеть. Проверки на эмуляторе в сеть
      не ходили и поймать этого не могли.
- [x] **Значок в шторке со сводкой подключений** — и служба переднего плана,
      которой он принадлежит: пока есть подключённые облака или идёт вход.
      Приложению со службой система сеть оставляет, так что и «Файлы»
      читают облако, пока OpenDisk свёрнут. По нажатию на значок — возврат
      в приложение.

Что осталось:

- [ ] Служба на Android 14 и новее (тип specialUse) на эмуляторе не
      проверялась: в CI эмулятор Android 10. Нужен прогон на образе поновее.

### 0.5.6 — облако = буква диска, файловый менеджер на телефоне, живучесть на Linux

- [x] **Windows: буква закреплена за облаком.** Выбирается из списка в
      настройках облака (видно, какие свободны, какие заняты системой и какие
      у других облаков), новому облаку свободная буква закрепляется сразу.
      Раньше буква подбиралась при каждом подключении, и от порядка
      подключения зависело, какое облако окажется на `D:`. Занята буква
      флешкой — облако не переезжает молча, а говорит, чем занято.
- [x] **Android: вкладки «Диски» и «Добавить»** и встроенный файловый
      менеджер. Облака, память телефона, карты памяти и — на телефонах с
      root — вся файловая система: открыть, отправить, скачать в телефон,
      копировать, вырезать и вставить между любыми дисками, переименовать,
      удалить, создать папку. Облако и память телефона читает один и тот же
      rclone, поэтому перенос между ними — один вызов без своего кода передачи.
- [x] **Доступ ко всем файлам** спрашивается при первом запуске, дальше —
      кнопкой на вкладке «Диски». root — при первом входе в «Корень системы».
- [x] **Linux: висящие точки FUSE снимаются сами** — перед подключением и
      после сбоя rclone. Раньше после аварийного завершения папка облака
      отвечала «Transport endpoint is not connected», и без `fusermount -u`
      руками облако не подключалось.
- [x] **Упавший rclone поднимается заново** (все системы), и подключённые
      облака возвращаются сами. Падает трижды за десять минут — перезапуски
      прекращаются, и видно его последние сообщения.
- [x] Непустая папка или чужое монтирование на месте точки — понятное
      сообщение вместо ошибки rclone.

Что осталось:

- [ ] Файловый менеджер на iPhone: те же вкладки и операции. Код iOS
      не собирается на машине без Xcode, делать его вслепую не стали.
- [ ] Инструментальные проверки файлового менеджера на эмуляторе
      (копирование облако ↔ память телефона через librclone).
- [ ] Ход копирования в процентах: сейчас виден только сам факт операции.

### 0.5.7 — вход заново без консоли, карточки облаков

- [x] **«Войти заново» у облака с истёкшим доступом.** rclone советует
      `rclone config reconnect`, которого у человека нет; теперь это кнопка,
      а имя, настройки и буква остаются прежними.
- [x] Карточка облака: имя и состояние на всю ширину, кнопки строкой ниже.
- [x] Буква закрепляется за всеми облаками при запуске, включая добавленные
      до 0.5.6.
- [x] Отключённый сетевой диск, который Windows помнит (`HKCU\Network`),
      держит букву — выбрать её для облака больше нельзя.

Что осталось:

- [ ] «Войти заново» на телефонах.

### 0.5.8 — облака на телефоне не пропадают, один значок в меню Linux

- [x] Служба со значком не гаснет при запуске приложения (подключённые — из
      настроек) и поднимается снова, если система выгрузила приложение.
- [x] Строка облака на телефоне только открывает его; «Файлы» и удаление — в «⋮».
- [x] Пакет для ALT убирает ярлык, оставшийся от пакета для Fedora; проверка
      установки требует ровно один ярлык.

Что осталось — **Android без значка в шторке**. Сейчас служба переднего
плана (и её значок) держится всё время, пока облако показано в «Файлах»,
ради одного: чтобы свёрнутому приложению не отрезали сеть. Задача —
сохранять вход и доступ к облакам без постоянного значка. План:

- [ ] **Проверить, нужна ли служба для «Файлов» вообще.** Когда «Файлы»
      читают облако, они держат связь с нашим поставщиком документов, и
      процесс получает важность того, кто его вызвал, — то есть
      приложения на экране. Такому процессу сеть, скорее всего, не режут,
      даже в режиме экономии трафика. Проверить на Android 10, 14, 15
      с экономией трафика и ограничением фона у производителя (Xiaomi,
      Samsung). Подтвердится — служба при показе в «Файлах» не нужна.
- [ ] **Вход через браузер без службы.** Сеть в фоне нужна только в момент
      обмена кода на токен, пока впереди вкладка браузера. Вариант —
      отложить обмен до возврата в приложение: сервер подтверждения rclone
      принимает код по локальному адресу (для этого сеть не нужна), а в сеть
      идти уже с экрана. Другой вариант — служба только на эти секунды, без
      постоянного значка.
- [ ] **Вход не должен слетать.** rclone обновляет токен при каждом
      обращении и сам пишет новый в `rclone.conf`; слетает вход, когда
      облаком долго не пользовались или сервис отозвал доступ (Google для
      своих приложений в режиме тестирования — через неделю). Нужны:
      «Войти заново» на телефоне, как на компьютере в 0.5.7, и фоновое
      обновление токенов раз в несколько дней через `WorkManager` —
      без значка, в окно, которое выдаёт система.
- [ ] Выключатель «значок в шторке» в настройках для тех, у кого всё
      работает и без него.

### 0.5.9 — обновление на Windows не ломает установку, облака на запись, ТВ и часы

- [x] **Обновление поверх работающего OpenDisk больше не ломает установку.**
      В 0.5.8 закрытие приложения стояло после снятия старой версии: все jar
      были заняты, их удаление откладывалось до перезагрузки, и после неё
      система удаляла уже новые файлы с теми же именами — от образа
      оставались три файла из пятидесяти. Теперь установщик сам закрывает
      OpenDisk и rclone из своего каталога до всех проверок (чужой rclone
      не трогает).
- [x] **Обновление не стирает настройки и автозапуск.** Старая версия при
      снятии в ходе обновления запускала уборку, как при удалении. Условие
      исправлено, а настройки, стёртые установщиками до 0.5.8, возвращаются:
      новый установщик сохраняет их до снятия старой версии. Автозапуск
      приложение сверяет с галкой в настройках при каждом старте.
- [x] **Android: облако на запись, как обычная папка.** Из «Файлов» и любого
      окна выбора файла: сохранить прямо в облако, отредактировать документ
      на месте, создать файл и папку, переименовать, перенести и скопировать
      (в том числе между облаками), удалить. Сохранённый файл уходит в облако,
      когда приложение его закрывает; не ушёл (нет сети) — досылается при
      следующем обращении. Скачанная копия обновляется, если файл в облаке
      изменился, — раньше отдавалась первая скачанная версия навсегда.
- [x] **Android TV и Wear OS.** Тот же apk: значок и баннер на главном экране
      телевизора, управление с пульта, поля от краёв экрана; на часах —
      самостоятельное приложение, компактные вкладки и отступы под круглый
      экран. Нет браузера для входа — об этом говорится прямо, а не молча.

Что осталось:

- [x] **32-битная ARM для приставок и часов.** librclone ревизии r2
      собрана и под armeabi-v7a; в выпуске отдельный `-arm32.apk`.
- [ ] Проверка на настоящих телевизорах (дома у владельца проекта).
- [ ] **Вход без браузера на телевизоре и часах.** Сейчас там подключаются
      только облака с логином и паролем (WebDAV, S3, SFTP) или с браузером.
      Нужен перенос облака с телефона (QR-код или файл).
- [x] Прогон поставщика документов на запись на эмуляторе
      (`DocumentsWriteTest`) и обновления поверх работающей копии на Windows
      (`verify-windows`, сценарий x64-over-running).
- [ ] Отдельный интерфейс для часов на Wear Compose — сейчас это
      телефонный интерфейс, ужатый под маленький экран.

### Windows на ARM (сделано в 0.5.3)

В 0.5.2 ARM-сборка не вышла — решили не задерживать выпуск. Корень был не
в самой сборке, а в том, что уборка вызывалась без предела по времени.

- [x] **Удаление больше не зависит от того, завершится ли лаунчер.**
      Установщик звал `OpenDisk.exe --cleanup` и ждал без конца; сборка 0.5.0
      для ARM не запускалась вовсе, и на ней это вешало и удаление, и
      обновление поверх — навсегда, до завершения процесса вручную. Теперь
      уборка идёт через `powershell` с пределом в минуту (`wix/Product.wxs`).
- [x] **ARM вернулась в сборку и в проверку.** Запуск проверяется строго,
      удаление тоже: на настоящей ARM-машине проходят установка, обновление
      запущенной копии, запуск и удаление.
- [x] **Цепочка обновления с прошлых версий на ARM не проверяется**, и это
      осознанно: прошлый выпуск для ARM — 0.5.0, а она не запускается, так
      что обновляться там не с чего.

### 0.6 — Android и iOS вместе

Обе мобильные платформы делаются одной веткой, а не по очереди. Причина не в
удобстве планирования: у них общее всё, кроме нативной обвязки, — тот же rclone,
тот же `RcloneClient`, тот же разбор путей. Делать их по очереди означало бы
дважды проходить одну и ту же дорогу.

Что уже готово:

- [x] Сборка `rclone` под Android через `gomobile bind` в CI, с публикацией
      библиотеки отдельным релизом и сверкой по SHA-256.
- [x] Транспорт поверх librclone; разбор ответов RC API общий с десктопом.
- [x] Инструментальные тесты на эмуляторе — включая ссылки на файлы (0.4.0).

Android — приложение выпущено в 0.5.0, осталось главное:

- [x] Интерфейс на Compose: облака, добавление, просмотр по папкам.
- [x] Ссылки на файлы — на общем с десктопом коде.
- [x] Раздача с учётом размера: ABI splits, а не один apk на всех.
- [x] Подпись постоянным ключом издателя (0.5.2): обновления встают поверх,
      а не требуют удалить приложение вместе с облаками. Ключ живёт в
      секретах CI и в резервной копии у владельца проекта — в репозитории
      его нет ни в каком виде.
- [x] `DocumentsProvider` (0.5.3) — официальный и единственный путь для
      облачных дисков на Android. Подключённое облако видно в системном
      «Файлы» и в любом окне выбора файла. Пока только на чтение.
- [x] Запись (0.5.9): сохранённый файл уходит в облако при закрытии, не
      ушедший досылается при следующем обращении.
- [ ] Фоновая работа через `WorkManager`, кэш по требованию. Сейчас файл
      скачивается целиком во временную папку: частичного чтения RC API
      не умеет.
- [ ] Сервисы с подтверждением в браузере: их поток требует вернуть человека
      из браузера обратно в приложение.

iOS:

- [x] Сборка `librclone` в `.xcframework` — отдельным workflow на macOS-раннере,
      как уже сделано для Android, с публикацией библиотеки отдельным релизом
      и сверкой по SHA-256. Сделано первым намеренно: в документации rclone про
      iOS сказано, что gomobile «должен работать», и это никем не проверялось, а
      раннер GitHub — единственный доступный способ узнать ответ. Собирается и
      под устройства (arm64), и под симулятор — без второго ничего нельзя даже
      запустить в Xcode.
- [x] Интерфейс на SwiftUI (0.5.3): облака, добавление, просмотр по папкам,
      ссылки. Не Compose Multiplatform: ради полутора сотен строк разбора
      JSON тащить Kotlin/Native не стоило, а общей и так остаётся сама RC API.
- [x] Транспорт поверх `.xcframework` (0.5.3) — свой, на Swift.
- [x] Сборка `.ipa` без подписи в CI (0.5.3) и честное описание того, чем
      её ставят и на сколько её хватает: [docs/IOS.md](docs/IOS.md).
- [ ] **File Provider Extension** — официальный и единственный путь у Apple
      к облаку в «Файлах». Расширению нужны права, которые выдаёт только
      Apple подписанному приложению, поэтому упирается в то же самое.

**Про установщики под телефоны.** На Android `.apk` выходит с 0.5.0, на iOS
`.ipa` — с 0.5.3, но без подписи: подписанный файл требует платной программы
разработчика Apple. Неподписанный ставится самоподписью того, кто ставит, —
и живёт семь дней до следующего обновления подписи.

**Два условия входа, которые не решаются кодом.** Про них лучше знать заранее,
чем упереться в них на середине:

1. **Платная программа разработчика Apple.** Без неё приложение не поставить
   даже себе больше чем на неделю, а в App Store — никак. Это не техническая
   задача, и обойти её нечем.
2. **Mac для проверки.** Собрать `.xcframework` и приложение можно на раннере
   GitHub, а вот запустить и посмотреть — нет. Ровно та же история, что с
   `.dmg`: он собирается каждый выпуск с самого начала и **ни разу никем не
   запускался**.

**Ограничение платформы, а не недоделка:** диск не будет виден произвольной
программе как папка ни на Android, ни на iOS. Обе системы такого не позволяют
никому, включая официальные клиенты Google Drive и Dropbox.

### 0.7 — то, что мешает пользоваться

Накопилось за время эксплуатации десктопа и никуда не делось.

- [ ] **Диагностика медленного облака.** Сейчас «тормозит» выясняется только
      замерами со стороны. Приложение должно само замечать, что список папки
      идёт десятки секунд, и говорить об этом с причиной.
- [ ] **Понятный отказ вместо ожидания.** Когда авторизация в браузере
      заканчивается ошибкой, приложение об этом не узнаёт и ждёт до таймаута.
- [ ] **Состояние диска честнее.** Сейчас видно «подключено» и «отключено»;
      между ними есть «подключается», «отвалилось», «сеть пропала».
- [ ] **Журнал для разбора проблем** — последние строки вывода rclone
      доступны из окна, а не только при падении запуска.

### 0.8 — macOS вживую

- [ ] Проверить `.dmg` на живой машине. Он собирается каждый выпуск с самого
      начала, но **ни разу никем не запускался** — Mac у разработчиков нет.
- [ ] macFUSE: обнаружение и подсказка по установке, как WinFsp на Windows.
- [ ] Автозапуск через `LaunchAgents` — единственная система, где его нет.

### 1.0 — когда можно считать законченным

- [ ] macOS проверена на живой машине.
- [ ] Android выпущен.
- [ ] Ни одного известного случая, когда приложение молча делает не то.

---

## Потом

- Шифрование содержимого на лету — у rclone это готовый бэкенд `crypt`.
- Синхронизация по расписанию, а не только монтирование по требованию.
- Срок жизни у ссылок: rclone умеет `expire`, и в API это уже проброшено —
  нужен способ задать его в интерфейсе, не превращая кнопку в форму.
- Общий слой бизнес-логики между десктопом и мобильными. Сейчас общие разбор
  ответов RC API и работа со ссылками; объединять остальное имеет смысл после
  того, как мобильные таргеты устоятся.
- Плагины для бэкендов, которых нет в rclone.

---

## Чего в карте нет намеренно

- **32-битные сборки.** Движок отрисовки под x86 не выпускается, а 32-битных
  JDK новее восьмой версии не существует.
- **Свой протокол или свой клиент облаков.** Всё это делает rclone, и делает
  лучше, чем получилось бы у нас.
- **Свой сервис коротких ссылок.** Ссылку на файл выдаёт сам сервис, у которого
  файл лежит, — только у него есть публичный адрес и только он отвечает за
  доступ к нему. Своя прослойка означала бы свой сервер, свои расходы и свою
  ответственность за чужие файлы.
- **Сроки.** Порядок важнее: каждый выпуск этой ветки появлялся из того,
  что нашлось при использовании, а не из плана.
