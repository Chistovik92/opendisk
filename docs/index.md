---
title: OpenDisk
description: Открытый клиент облачных дисков для Windows, Linux и macOS
---

# OpenDisk

Подключает облачные хранилища как обычные диски. Файлы не скачиваются целиком:
вы открываете папку в проводнике, а нужное подгружается по требованию.

Бесплатная альтернатива Disk-O и Air Explorer. Работает на движке
[rclone](https://rclone.org/) — больше 70 облачных сервисов, — со своим
интерфейсом на Kotlin и Compose Multiplatform.

> **Ранняя стадия разработки.** Основное работает и проверено на Windows
> и Linux, но шероховатости встречаются. Что именно не сделано — честно
> перечислено в конце страницы.

## Скачать

Все сборки — на [странице релизов](https://github.com/Chistovik92/opendisk/releases).

| Система | Файл |
|---|---|
| Windows 10/11 | `OpenDisk-*-x64.exe` |
| Windows 11 на ARM (Snapdragon и подобные) | `OpenDisk-*-arm64.exe` |
| Debian, Ubuntu, Mint, Astra | `opendisk_*_amd64.deb` |
| ALT Linux, Simply Linux | `opendisk-*-alt1.x86_64.rpm` |
| Fedora, RHEL, RED OS, openSUSE | `opendisk-*-1.x86_64.rpm` |
| Любой другой Linux | `OpenDisk-*-x86_64.AppImage` |
| macOS на Apple Silicon | `OpenDisk-*-arm64.dmg` — **не проверялся** |
| macOS на Intel | `OpenDisk-*-x64.dmg` — **не проверялся** |
| Android | `OpenDisk-*-arm64.apk` или `-universal.apk` |
| iPhone, iPad | `OpenDisk-*.ipa` — **без подписи Apple**, см. [IOS.md](IOS.md) |

Каждый установщик перед выпуском ставится на настоящую систему: Windows
(x64 и ARM) — с обновлением со старых версий, запуском и удалением; Linux —
в чистых Debian, Ubuntu, Fedora, openSUSE и ALT тем самым скриптом, что
ниже; Android — на эмуляторе. Про `.dmg` и `.ipa` честно: они только
собираются — ни Mac, ни iPhone для проверки у разработчиков нет.

### Linux одной командой

```bash
curl -fsSL https://raw.githubusercontent.com/Chistovik92/opendisk/main/scripts/install.sh | sh
```

Скрипт определит дистрибутив, скачает пакет именно под него, сверит
контрольную сумму и установит.

## Что умеет

- **Подключение облака как диска** — Яндекс.Диск, Облако Mail.ru, Google Диск,
  Dropbox, OneDrive нажатием плитки; всё остальное — через полный список
  бэкендов rclone с поиском
- **Выбор, что хранить на диске** — от «ничего не кэшировать» до «скачивать
  открытое целиком». Это ответ на главный вопрос: скачивать всё или только то,
  с чем работаешь
- **Изменение подключения** — адрес сервера, логин или пароль меняются без
  пересоздания облака: браузерную авторизацию проходить заново не нужно
- **Обновление из приложения** — версия видна в заголовке, новая скачивается
  и ставится по кнопке со сверкой контрольной суммы
- **Удаление без следов** — свои файлы приложение убирает само, а список облаков
  и драйвер WinFsp удаляются по отдельной галочке: это ваши данные и чужая
  программа соответственно
- **Автозапуск и автоподключение** — диски появляются сами после входа в систему
- **Ограничение скорости**
- **Русский и английский** интерфейс, по умолчанию как в системе
- **Оформление из системы** — светлое или тёмное, с её же цветом выделения;
  на Android 12 и новее приложение берёт палитру телефона целиком
- **На телефоне облако видно в «Файлах»** (Android): подключённое облако
  доступно любому приложению через системный выбор файлов
- **Ничего не нужно доустанавливать**: rclone внутри, драйвер файловой системы
  для Windows ставится из приложения по кнопке

## Документация

- [Руководство пользователя](USAGE.md) — установка, подключение облаков,
  настройки, разбор проблем
- [Как это устроено](ARCHITECTURE.md) — архитектура для тех, кто хочет
  разобраться или доработать
- [Сборка из исходников](SETUP.md)

## Чего пока нет

- **macOS не проверялась.** Пакет собирается автоматически, но никто его
  не запускал — Mac у разработчиков нет
- **На iPhone приложение без подписи Apple.** Ставится самоподписью через
  AltStore или Sideloadly, и такая подпись живёт семь дней — [подробно](IOS.md)
- **На телефонах облако доступно только на чтение.** Заливка в облако из
  чужого приложения идёт в фоне, с обрывами и дозагрузкой — это отдельная
  работа
- **В «Файлах» облако видно только на Android.** На iOS для этого нужно
  расширение, а расширению нужны права, которые Apple выдаёт лишь
  подписанному приложению
- **32-битных сборок не будет**: движок отрисовки не выпускается под x86,
  а 32-битных JDK новее восьмой версии не существует

## Исходный код

[github.com/Chistovik92/opendisk](https://github.com/Chistovik92/opendisk) —
лицензия MIT. Встроенный rclone тоже MIT, установщик WinFsp — GPLv3
со специальным исключением.

Нашли ошибку или чего-то не хватает —
[заведите issue](https://github.com/Chistovik92/opendisk/issues).
